{
  "name": "magnolia-engine",
  "version": "0.4.1",
  "description": "WebGPU/WebGL rendering engine",
  "main": "app.ts",
  "scripts": {
    "start": "npm run install-dependencies && npm run dev",
    "install-dependencies": "npm i",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist"
  },
  "author": "Antero Lindell",
  "license": "ISC",
  "dependencies": {
    "@webgpu/types": "^0.1.40",
    "gl-matrix": "^3.4.3",
    "performance-now": "^2.1.0",
    "vite": "^5.1.3"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
